# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'EasyMapper' do
  use_frameworks!

  # Pods for EasyMapper
  pod 'AlamofireObjectMapper', '~> 4.0.0'

  target 'EasyMapperTests' do
    inherit! :search_paths
  end
end
